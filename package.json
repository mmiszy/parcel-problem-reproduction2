{
  "name": "reproduction",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "clean:client": "rm -rf ./.parcel-cache && rm -rf ./dist/client",
    "dev:client": "cd ./client && parcel serve ./src/index.html",
    "build:client": "npm run clean:client && cd ./client && parcel build ./src/index.html --dist-dir ../dist/client"
  },
  "devDependencies": {
    "parcel": "2.0.0-nightly.627"
  },
  "engines": {
    "node": "14.x.x",
    "npm": "7.x.x"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "dependencies": {
    "@ant-design/icons": "4.5.0",
    "antd": "4.14.0",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
